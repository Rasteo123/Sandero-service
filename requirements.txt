# Нужен только для пересборки корпуса (scripts/ingest.py):
# читает текстовый слой PDF и оглавление, по которому нарезаются
# атомарные единицы сервисного мануала.
# Поиск (scripts/search.py) работает на голой стандартной библиотеке python3.
pymupdf>=1.24
